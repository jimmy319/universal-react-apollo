# Build an "Hello World" universal app

## Getting start

Compile the library source before running

```
npm i

npm start
```

Now that your universal app is running at `http://localhost:3000/greeting/visitor`
